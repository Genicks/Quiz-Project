
  // gameLevel(one, temp, array, &totalScore, &fullScore);
  // gameLevel(one, temp, array, &totalScore, &fullScore);
  // gameLevel(one, temp, array, &totalScore, &fullScore);
  // gameLevel(one, temp, array, &totalScore, &fullScore);
  // gameLevel(one, temp, array, &totalScore, &fullScore);
  // gameLevel(one, temp, array, &totalScore, &fullScore);